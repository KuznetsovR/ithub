import { BrowserRouter } from "react-router-dom";
import { Routes } from "react-router";
import React from "react";

export const App = () => {
  return (
    <BrowserRouter>
      <Routes>

      </Routes>
    </BrowserRouter>
  );
}
